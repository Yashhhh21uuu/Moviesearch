<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Movie Search App</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #74ebd5, #9face6);
      color: #333;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }
    .container {
      max-width: 800px;
    }
    .card {
      margin-top: 20px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }
    .poster-img {
      max-height: 400px;
      object-fit: cover;
    }
    .btn-primary {
      background-color: #6c63ff;
      border-color: #6c63ff;
    }
    .btn-primary:hover {
      background-color: #5145cd;
      border-color: #5145cd;
    }
  </style>
</head>
<body>
  <div class="container text-center">
    <h1 class="display-4 text-white mb-4">Movie Search App</h1>

    <!-- Search Form -->
    <form action="/search" method="POST" class="d-flex justify-content-center mb-4">
      <div class="input-group" style="max-width: 500px;">
        <input type="text" name="movieTitle" class="form-control" placeholder="Enter movie title" required>
        <button type="submit" class="btn btn-primary">Search</button>
      </div>
    </form>

    <!-- Error Message -->
    <% if (error) { %>
      <div class="alert alert-danger" role="alert">
        <%= error %>
      </div>
    <% } %>

    <!-- Movie Details -->
    <% if (movie) { %>
      <div class="card mx-auto">
        <div class="row g-0">
          <div class="col-md-4">
            <img src="<%= movie.Poster %>" alt="<%= movie.Title %> Poster" class="poster-img img-fluid rounded-start">
          </div>
          <div class="col-md-8">
            <div class="card-body">
              <h5 class="card-title"><strong><%= movie.Title %></strong> (<%= movie.Year %>)</h5>
              <p class="mb-1"><strong>Rating:</strong> <%= movie.imdbRating %> / 10</p>
              <p class="mb-1"><strong>Genre:</strong> <%= movie.Genre %></p>
              <p class="mb-1"><strong>Director:</strong> <%= movie.Director %></p>
              <p class="mb-1"><strong>Plot:</strong> <%= movie.Plot %></p>
              <p class="mb-1"><strong>Box Office:</strong> <%= movie.BoxOffice %></p>
              <p class="mb-1"><strong>Awards:</strong> <%= movie.Awards %></p>
              <p><strong>IMDB ID:</strong> <%= movie.imdbID %></p>
            </div>
          </div>
        </div>
      </div>
    <% } %>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
